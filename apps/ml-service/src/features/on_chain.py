import pandas as pd


async def fetch_on_chain_features(asset: str) -> pd.DataFrame:
    """Fetch on-chain metrics as features."""
    # TODO: Query on-chain metrics from database
    return pd.DataFrame()
